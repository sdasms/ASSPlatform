<?xml version="1.0"?>
<configuration>
  <configSections>
  </configSections>
  <connectionStrings>
    <add name="HostTest.Properties.Settings.AssDBConnectionString" connectionString="Data Source=172.17.10.147;Initial Catalog=AssDB;Integrated Security=True" providerName="System.Data.SqlClient"/>
  </connectionStrings>
  <!-- 部署服务库项目时，必须将配置文件的内容添加到 
  主机的 app.config 文件中。System.Configuration 不支持库的配置文件。-->
  <system.serviceModel>
    <bindings>
      <netTcpContextBinding>
        <binding name="FaultBinding" maxBufferPoolSize="200000" maxBufferSize="200000" maxReceivedMessageSize="200000">
          <readerQuotas maxDepth="32" maxStringContentLength="200000 " maxArrayLength="200000 " maxBytesPerRead="4096" maxNameTableCharCount="16384" />
          <security mode="Message">
            <message clientCredentialType="UserName"/>
          </security>
        </binding>
      </netTcpContextBinding>
    </bindings>
    <services>
      <service name="FaultReportServer.FaultReportService" behaviorConfiguration="Cert">
        <endpoint address="" binding="netTcpContextBinding" bindingConfiguration="" name="FaultReportService" contract="FaultReportServer.IFaultReportContract">
          <identity>
            <dns value="localhost"/>
          </identity>
        </endpoint>
        <endpoint address="mex" binding="mexTcpBinding" contract="IMetadataExchange"/>
        <host>
          <baseAddresses>
            <add baseAddress="net.tcp://localhost:88/FaultReportServer/"/>
          </baseAddresses>
        </host>
      </service>
    </services>
    <behaviors>
      <serviceBehaviors>
        <behavior name="Cert">
          <!-- 为避免泄漏元数据信息，
          请在部署前将以下值设置为 false 并删除上面的元数据终结点  -->
          <serviceMetadata httpGetEnabled="False"/>
          <!-- 要接收故障异常详细信息以进行调试，
          请将以下值设置为 true。在部署前设置为 false 
            以避免泄漏异常信息-->
          <serviceDebug includeExceptionDetailInFaults="False"/>
          <serviceCredentials>
            <serviceCertificate findValue="SvrSecurity" storeLocation="LocalMachine" storeName="My" x509FindType="FindBySubjectName"/>
            <userNameAuthentication userNamePasswordValidationMode="Custom" customUserNamePasswordValidatorType="FaultReportServer.MyUsernamePasswordValidator, FaultReportServer"/>
          </serviceCredentials>
        </behavior>
      </serviceBehaviors>
    </behaviors>
  </system.serviceModel>
<startup><supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.0"/></startup></configuration>
